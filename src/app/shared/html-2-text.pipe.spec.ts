import { Html2TextPipe } from './html-2-text.pipe';

describe('Html2TextPipe', () => {
  it('create an instance', () => {
    const pipe = new Html2TextPipe();
    expect(pipe).toBeTruthy();
  });
});
