import { TripleSlidePipe } from './triple-slide.pipe';

describe('TripleSlidePipe', () => {
  it('create an instance', () => {
    const pipe = new TripleSlidePipe();
    expect(pipe).toBeTruthy();
  });
});
