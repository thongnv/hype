import { CurateListPipe } from './curate-list.pipe';

describe('CurateListPipe', () => {
  it('create an instance', () => {
    const pipe = new CurateListPipe();
    expect(pipe).toBeTruthy();
  });
});
